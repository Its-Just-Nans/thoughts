---
interface Props {
    date: Date;
    className?: string;
}

const { date, className } = Astro.props;

const fmt = date.toISOString().split("T")[0];
const datetime = date.toISOString();
---

<time class={className} datetime={datetime}>{fmt}</time>
