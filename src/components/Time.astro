---
interface Props {
    date: Date;
    className?: string;
}

const { date, className } = Astro.props;

const fmt = date.toISOString().split("T")[0];
---

<time class={className} datetime={date.toISOString()}>{fmt}</time>
